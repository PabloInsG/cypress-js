/// <reference types="cypress" />

context('Dummy test 3 (should pass)', () => {
  it(`Should just pass`, () => {
    expect(true).to.equal(true);
  });
  it(`Should also pass`, () => {
    expect(true).to.equal(true);
  });
});
